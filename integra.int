// This is an example application written in Integra.

equation genesis() {
    print("abc %d\n", fn(1,2,3));
    print("test\n");

    x := 3;
    if x == 3 {
        v := 1;
        write("%d -> %d\n", 3 * (2 + -1) - 0x18 + v, 0x48);
        v = 2;
        x = 1;
    }

    return 0;
}

equation fn(S, N, K) {
    S = 1;
    return 5 + +3;
}